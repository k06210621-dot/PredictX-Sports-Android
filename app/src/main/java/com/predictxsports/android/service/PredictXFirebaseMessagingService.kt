package com.predictxsports.android.service

/**
 * PredictXFirebaseMessagingService — 已停用（Android 不使用推送功能）
 *
 * 原本處理 FCM token 更新與推播訊息接收。
 * 已移除 Firebase 依賴，此類別不再擴展 FirebaseMessagingService。
 */
class PredictXFirebaseMessagingService {
    // no-op
}