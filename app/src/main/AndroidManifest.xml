<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Play Console 廣告 ID 宣告要求：即使未使用 AdMob，也需宣告此權限以避免 Play Console 阻擋上傳。
         此 App 目前並未使用 AdMob / play-services-ads，但保留宣告以符合 Play Console 檢查邏輯。 -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <application
        android:name=".PredictXApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="PredictX Sports"
        android:supportsRtl="true"
        android:theme="@style/Theme.PredictX">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PredictX">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PredictX.Splash">
        </activity>

    </application>
</manifest>